/**
 * Small camera-viewfinder corner brackets — the one recurring signature
 * motif used sparingly across the hero portrait, the showreel, and the
 * work grid on hover, tying the UI back to the subject: framing a shot.
 */
export default function FrameCorners() {
  return (
    <div className="frame-corners" aria-hidden="true">
      <span></span>
      <span></span>
      <span></span>
      <span></span>
    </div>
  );
}
